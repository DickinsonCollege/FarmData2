import { Selection } from '../core/selection';
import { ICommand, ICursorStateComputerData, IEditOperationBuilder } from '../editorCommon';
import { ITextModel } from '../model';
import { EditorAutoIndentStrategy } from '../config/editorOptions';
export interface IShiftCommandOpts {
    isUnshift: boolean;
    tabSize: number;
    indentSize: number;
    insertSpaces: boolean;
    useTabStops: boolean;
    autoIndent: EditorAutoIndentStrategy;
}
export declare function cachedStringRepeat(str: string, count: number): string;
export declare class ShiftCommand implements ICommand {
    static unshiftIndent(line: string, column: number, tabSize: number, indentSize: number, insertSpaces: boolean): string;
    static shiftIndent(line: string, column: number, tabSize: number, indentSize: number, insertSpaces: boolean): string;
    private readonly _opts;
    private readonly _selection;
    private _selectionId;
    private _useLastEditRangeForCursorEndPosition;
    private _selectionStartColumnStaysPut;
    constructor(range: Selection, opts: IShiftCommandOpts);
    private _addEditOperation;
    getEditOperations(model: ITextModel, builder: IEditOperationBuilder): void;
    computeCursorState(model: ITextModel, helper: ICursorStateComputerData): Selection;
}
//# sourceMappingURL=shiftCommand.d.ts.map