import { ICodeEditor } from '../../../browser/editorBrowser';
import { IPosition } from '../../../common/core/position';
import { CompletionItem } from '../../../common/languages';
import { IEditorWorkerService } from '../../../common/services/editorWorker';
export declare abstract class WordDistance {
    static readonly None: {
        distance(): number;
    };
    static create(service: IEditorWorkerService, editor: ICodeEditor): Promise<WordDistance>;
    abstract distance(anchor: IPosition, suggestion: CompletionItem): number;
}
//# sourceMappingURL=wordDistance.d.ts.map