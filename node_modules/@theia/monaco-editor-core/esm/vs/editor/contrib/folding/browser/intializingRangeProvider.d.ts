import { CancellationToken } from '../../../../base/common/cancellation';
import { ITextModel } from '../../../common/model';
import { FoldingRegions, ILineRange } from './foldingRanges';
import { RangeProvider } from './folding';
export declare const ID_INIT_PROVIDER = "init";
export declare class InitializingRangeProvider implements RangeProvider {
    private readonly editorModel;
    readonly id = "init";
    private decorationIds;
    private timeout;
    constructor(editorModel: ITextModel, initialRanges: ILineRange[], onTimeout: () => void, timeoutTime: number);
    dispose(): void;
    compute(cancelationToken: CancellationToken): Promise<FoldingRegions>;
}
//# sourceMappingURL=intializingRangeProvider.d.ts.map