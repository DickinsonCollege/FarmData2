import { CancellationToken } from '../../../../base/common/cancellation';
import { ITextModel } from '../../../common/model';
import { FoldingMarkers } from '../../../common/languages/languageConfiguration';
import { ILanguageConfigurationService } from '../../../common/languages/languageConfigurationRegistry';
import { FoldingRegions } from './foldingRanges';
import { RangeProvider } from './folding';
export declare const ID_INDENT_PROVIDER = "indent";
export declare class IndentRangeProvider implements RangeProvider {
    private readonly editorModel;
    private readonly languageConfigurationService;
    private readonly maxFoldingRegions;
    readonly id = "indent";
    constructor(editorModel: ITextModel, languageConfigurationService: ILanguageConfigurationService, maxFoldingRegions: number);
    dispose(): void;
    compute(cancelationToken: CancellationToken, notifyTooManyRegions: (maxRegions: number) => void): Promise<FoldingRegions>;
}
export declare class RangesCollector {
    private readonly _notifyTooManyRegions?;
    private readonly _startIndexes;
    private readonly _endIndexes;
    private readonly _indentOccurrences;
    private _length;
    private readonly _foldingRangesLimit;
    constructor(foldingRangesLimit: number, _notifyTooManyRegions?: ((maxRegions: number) => void) | undefined);
    insertFirst(startLineNumber: number, endLineNumber: number, indent: number): void;
    toIndentRanges(model: ITextModel): FoldingRegions;
}
export declare function computeRanges(model: ITextModel, offSide: boolean, markers?: FoldingMarkers, foldingRangesLimit?: number, notifyTooManyRegions?: (maxRegions: number) => void): FoldingRegions;
//# sourceMappingURL=indentRangeProvider.d.ts.map