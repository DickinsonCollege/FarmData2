import { Disposable } from '../../../base/common/lifecycle';
import { IPointerHandlerHelper, MouseHandler } from './mouseHandler';
import { IMouseTarget } from '../editorBrowser';
import { EditorMouseEvent } from '../editorDom';
import { ViewController } from '../view/viewController';
import { ViewContext } from '../../common/viewModel/viewContext';
/**
 * Currently only tested on iOS 13/ iPadOS.
 */
export declare class PointerEventHandler extends MouseHandler {
    private _lastPointerType;
    constructor(context: ViewContext, viewController: ViewController, viewHelper: IPointerHandlerHelper);
    private onTap;
    private onChange;
    _onMouseDown(e: EditorMouseEvent): void;
}
export declare class PointerHandler extends Disposable {
    private readonly handler;
    constructor(context: ViewContext, viewController: ViewController, viewHelper: IPointerHandlerHelper);
    getTargetAtClientPoint(clientX: number, clientY: number): IMouseTarget | null;
}
//# sourceMappingURL=pointerHandler.d.ts.map