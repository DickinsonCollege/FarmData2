import { GitProgressParser } from './git';
/**
 * A class that parses output from `git checkout --progress` and provides
 * structured progress events.
 */
export declare class CheckoutProgressParser extends GitProgressParser {
    constructor();
}
